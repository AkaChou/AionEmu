# 任务 18501/18503 客户端验收记录

quest: 18501、18503；相关对照任务 28501、28503 已随同一修复提交

user acceptance confirmation: 用户于 2026-08-27 回复“验收完成”，未限定分支或步骤；按规则视为本次 18501/18503 完整任务流程验收完成。

server launch mode: not captured

repository commit: `4e5a1a8dc031a3963632bcab7b7b4210ceef082b` (`fix(quest): align Haramel item-collecting NPC routes`)

working tree: dirty；验收对应的四个任务 XML 与两份回归测试已包含在上述提交，其他未提交文件不属于本次修复。

Aion 5.8 client/data provenance: Aion 5.8 客户端；页面与动作证据取自仓库内 `docs/quest/client-dialog-mapping/` 清单及旧任务模板；本次未重新采集客户端包哈希。

npc template/object: 18501/28501 接取 NPC `799522`、交付 NPC `799523`，交互物 `700833`、`700951`；18503 接取 NPC `799523`、交付 NPC `203166`，交互物 `700834`；28503 接取 NPC `799523`、交付 NPC `804605`，交互物 `700834`。运行时 object ID not captured。

map/instance: 哈拉梅尔副本 `300200000`；实际运行 world/instance ID not captured。

steps:

1. 18501/28501：接取任务后使用 `700833`、`700951`，分别收集对应任务物品，再回到 `799523` 交付并领取奖励。
2. 18503/28503：接取任务后使用 `700834` 收集对应任务物品，再到各自交付 NPC（`203166`/`804605`）完成交付并领取奖励。

source state/status/vars: 任务开始为 `START var0=0`；交付后为 `REWARD var0=1`；完成后为 `COMPLETE var0=0`。

action/page/button: 交互物使用 `ACTION_ITEM_USE` 门控及 `USE_OBJECT(-1)`/`START_DIALOG(0)` 路由；NPC 交付链使用 `QUEST_SELECT(31)`、`CHECK_USER_HAS_QUEST_ITEM(39)`、`CHECK_USER_HAS_QUEST_ITEM_SIMPLE(20002)`、`SET_SUCCEED(10255)` 及奖励选择动作。

expected response: 交互物仅在 `START` 状态可用并产生对应任务掉落；集齐物品后由正确交付 NPC 校验并事务内移除物品，提交后同步任务状态、显示奖励页，随后完成任务。

actual response: 用户确认验收完成，未报告交互物无响应、错误交付 NPC 或奖励流程异常。

startup health: 四个 XML 均通过 `xmllint --noout --schema quest_definition.xsd`，`git diff --check` 通过；本会话未运行 Maven 专项测试及 production catalog/whitelist 门禁，用户未报告 `Can't initialize typed quest engine`、`QuestCompilationException`、`AMBIGUOUS_TRANSITION` 或 production catalog compile failure。

runtime logs: not captured

protocol trace: not captured

screenshots/recordings and SHA-256: not captured

acceptance status: ACCEPTED_EXISTING_PATTERN

matched Pattern: `STATE_GATED_ACTION_ITEM_USE_FLOW`（`START` 状态、`ACTION_ITEM_USE`、交互物掉落与后续任务物品链）；`OBJECT_ACTION_AND_QUEST_SELECT_ID_SPACE`（交互物动作与 NPC `QUEST_SELECT` 分离）；`MULTI_NPC_HANDOFF_REWARD_OWNER`（接取 NPC 与交付/领奖 NPC 独立且交付 owner 唯一）。代表提交 `c02c8722e`、`0786d4126`、`598deb98f`；代表测试 `EarlyElyosQuestRegressionTest#stolenVillageSealUsesTheItemStackOnlyAfterAcceptance`、`EarlyElyosQuestRegressionTest#recoveredVillageSealUsesTheItemStackOnlyAfterAcceptance`、`Quest1163ClientDialogAlignmentTest#followsTheRetailPotionHandoffAndRewardOwner`。

remaining risks: Maven 专项测试、production catalog/whitelist 未在本会话运行；运行时 object/instance ID、启动日志、抓包和截图未捕获；28501/28503 为同一 XML 修复批次中的阵营对照任务，未单独记录客户端路径。
