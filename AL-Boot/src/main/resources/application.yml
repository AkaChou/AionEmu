spring:
  main:
    banner-mode: "off"
    web-application-type: none

aion:
  services:
    game:
      enabled: true
    login:
      enabled: true
    chat:
      enabled: false
    transport:
      # Keep legacy-nio until game/login/chat endpoint adapters are migrated to Netty 4.
      mode: legacy-nio
