# 真端副本完整审计

本文记录本轮按真端 world、AI Pattern、条件出生、静态出生和掉落数据核对的结果。

## Fire Temple（320100000）

### 真端证据

- `58Server/Map/Worlds/iddf2_dflame/world_N.xml` 的五个独立 named spawn group 提供了 Fire Temple 稀有出生点：
  `212839`、`212840`、`212841`、`212842`、`212843` 的选择概率分别为 `333/1000`、`333/1000`、`333/1000`、`333/1000`、`3333/10000`。
- 同一 world 在 `421.993530,93.189148,122` 以 `1000/10000` 选择 `214621`；该点没有原 Handler 的 `212845` 分支。
- `298.709503,89.422447,131` 的真端组只有 `214094`，因此该点使用普通静态出生，不写入 `212845` 分支。
- `58Server` Fire Temple 掉落表已包含 `212840` 的 `188051411`、`212842` 的 `188051412` 以及 `833523/833524/833525` 的 `188052826` 和公共掉落；Handler 注入属于重复私服逻辑。

### 已完成

- 将六个真端稀有 NPC 写入 `320100000_Fire_Temple.xml`，使用 `alternate_id/select_prob` 表达真端概率；补回纯 `214094` 的真端静态点。
- 删除 Handler 的掉落注册、实例创建随机出生和旧自定义物品注入。
- 合并 Kromede 三份重复公告逻辑。

### 保留的 Handler ownership

- 真端 world 没有静态生成 `833523`、`833524`、`833525` 宝箱；Handler 在 `212846` 或 `214621` 死亡后按三档随机生成宝箱并发送对应钥匙提示，因此该流程保留。

### 验证范围

- `FireTempleRetailMigrationTest` 锁定六个替代出生、纯 `214094` 点、无 `212845` 特殊点、Handler 无重复掉落/实例创建逻辑且保留三档宝箱流程。
- 运行 Fire 专项测试、既有迁移/条件出生/Pattern AI 测试、静态出生 XSD 和 `git diff --check`。

## 未闭环

- 其余生产副本仍需按同样 ownership 证据逐图处理；单图完成不代表 139 个区域全部完成。
- 客户端协议、基础倍率和 GEO/PATH 压测不在本批次范围内。
