# 收集/交付类修复 真机验收清单（QE-031 追加批次）

日期：2026-09-17　适用版本：本分支 `quest`（含 `207f79f37` 与本批未提交修复）

## 一、零进度交付（本批新修 7 个：交付边补回 `has-item` + `remove-item`）

| 任务 | 交付 NPC / 动作 | 任务道具 | 需要数量 | 验收要点 |
|---|---|---|---|---|
| 1932 A Matter Of Reputation | 203893 / SETPRO1 | 182206008 | 1 | 0 个时不能推进到领奖；集齐后交付成功且道具被扣除 |
| 3547 | 204647 / SETPRO1 | 182215334 | 10 | 同上（9 个应被拦下） |
| 14121 | 204032 / SETPRO1 | 182215479 | 5 | 同上 |
| 14201 | 798155 / SETPRO1 | 182215468 | 1 | 同上 |
| 24121 | 804606 / SETPRO1 | 182215470 | 1 | 同上 |
| 24152 | 802364 / SETPRO1 | 182215461 | 1 | 同上（真端 COLLECT_ITEM NPC 亦为 802364） |
| 24242 | 804606 / SETPRO1 | 182215583 | 1 | 同上 |

判定「未集齐」时客户端应停在对话（不关窗、不进奖励窗），且不会下发 `SM_QUEST_ACTION 状态=REWARD`。

## 一之二、多 NPC 变体任务（第三批 6 个，17 条交付边）

同一任务由多个 NPC 交付（种族/职业/区域变体），每条交付边都必须校验并扣除自家任务道具：

| 任务 | 交付 NPC（动作） | 道具 | 数量 |
|---|---|---|---|
| 2232 | 203613 / 203622 / 700061（SETPRO1） | 182203224 | 9 |
| 2239 | 203613 / 203630（SETPRO1） | 182203228 | 3 |
| 2289 | 203616 / 203618（SETPRO2） | 182203016 | 1 |
| 3013 | 798132 / 798146（SET_SUCCEED） | 182208008 | 1 |
| 3088 | 798132 / 798166 / 798201 / 798202 / 798204（SETPRO2） | 182208064 | 1 |
| 4542 | 204743 / 204768 / 204808（SETPRO2） | 182215329 | 1 |

验收要点：**逐个 NPC 变体**都要验证"0 个被拦下 / 集齐后交付成功且扣除"，避免只测到其中一个入口。

## 一之三、NPC_REPORT 简写交付（第四批 51 个任务 / 79 条路由）

这些任务的交付按钮此前**无条件进入奖励窗**。修复后：未集齐 → 显示"物品不足"页并留在当前阶段；集齐 → 进入奖励窗且道具被扣除。

重点抽验（多 NPC 变体 / 多道具）：
`11003`(2 NPC) · `2232`(3) · `2307`(3, 4 件) · `3088`(5) · `3096`(5, 4 件) · `30213`(3) · `25052`(3) · `4012`(3) · `4542`(3) · `2527`(2) · `2237`(2) · `15670`(4 件) · `15072`(2 件) · `4940`(4 件) · `16942`(源阶段 s1) · `30003`/`30103`(源阶段 step1)

完整清单（含每条路由的源节点与 NPC）：`.agents/summary/quest/item-producer-scan/item-handin-route-gaps.tsv`（记录的是修复前命中的任务集合与当前剩余项）。

**注意**：`25013/25022/25050/25062/25073/25080/25081/25094/25306/25526/25532/25535/25538` 的 `reward→reward` 路由**故意不校验**（重新打开奖励窗），验收时若发现这些任务在奖励阶段无法再次打开奖励窗，属门禁边界问题，请回报。

## 二、跨任务道具错配（已提交 7 个）

| 任务 | 场景 | 验收要点 |
|---|---|---|
| 15010 | 两种采集物 5+3 | 两种都需集齐；交付各扣 5/3 |
| 15012 / 15043 / 15070 | 单一采集物 | 交付按 5 / 7 / 10 扣；不再出现"背包有道具却提示不足" |
| 51021 | 采集物 3 | 交付扣 3（原校验的是 51018 的道具） |
| 28836 / 28838 | 收集进度条 | 每次拾取任务道具后客户端进度应刷新（原监听邻居任务道具，进度恒为 0） |

## 三、日志取证（与你的排查日志同格式）

关键行：`CM_DIALOG_SELECT`（动作=31 接取 / 20000 领奖 / 1009 交付校验）、`SM_DIALOG_WINDOW 下发页`、`SM_QUEST_ACTION 状态/步数`。

期望：未集齐时**不出现**进入 REWARD 的 `SM_QUEST_ACTION 状态=3`（除非任务本身分阶段）；交付成功时出现 `状态=3 步数=N` 且随后 `targetObj=0 questId=0 下发页=0/10`。

## 四、回滚口径

若某任务被误判（正常流程被 `has-item` 拦住），只需移除该任务交付边上的 `<conditions>/<actions>` 两行并记录证据；门禁 `QuestItemSourceContractGateTest` 会同步报错，需一并更新回归列表。
