package com.aionemu.gameserver.model.instancerift;

/**
 * 副本裂隙状态类型枚举。
 * Instance Rift State Type enumeration.
 *
 * @author Rinzler (Encom)
 */

public enum InstanceRiftStateType {
	/** 打开 / Open. */
	OPEN, CLOSED;
}
