package com.aionemu.gameserver.model.landing;

/**
 * 登陆点 Enum 枚举。
 * Landing Points Enum enumeration.
 */

public enum LandingPointsEnum {
	/** 要塞。 / Siege. */
	SIEGE,
	/** 基地。 / Base. */
	BASE,
	/** 指挥官。 / Commander. */
	COMMANDER,
	/** 圣物。 / Artifact. */
	ARTIFACT,
	/** 任务。 / Quest. */
	QUEST,
	/** 纪念碑。 / Monument. */
	MONUMENT,
	/** 设施。 / Facility. */
	FACILITY
}
