package com.aionemu.gameserver.questEngine.definition;

/** Closed set of system messages currently needed by migrated quest owners. */
public enum QuestSystemMessage {
	QUEST_FAILED,
	WAREHOUSE_FULL_INVENTORY
}
