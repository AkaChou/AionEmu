package com.aionemu.gameserver.questEngine.definition;

/** Defines whether a learned recipe belongs to the quest lifecycle or to the player permanently. */
public enum QuestRecipeOwnership {
	QUEST_OWNED,
	PERMANENT
}
