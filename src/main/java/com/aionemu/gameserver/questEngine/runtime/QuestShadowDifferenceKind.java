package com.aionemu.gameserver.questEngine.runtime;

/** Independent dimensions used by shadow gates. */
public enum QuestShadowDifferenceKind {
	ROUTE,
	CONDITION,
	VARIABLES,
	ACTION,
	REWARD,
	PROTOCOL,
	RESULT_CONSUMPTION
}
