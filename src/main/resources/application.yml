spring:
  main:
    banner-mode: "off"
    web-application-type: none

aion:
  game:
    network:
      external-ip: 192.168.1.18
    startup:
      progress:
        enabled: true
  services:
    game:
      enabled: true
    login:
      enabled: true
    chat:
      enabled: true
    transport:
      mode: netty
